# Uploaded files don't belong in version control — only the folder structure does.
# The site logo/footer images used to live here as a tracked exception, but they're
# static branding, not user-uploaded content, so they've moved to frontend/public/
# instead — served directly by Vercel, no backend dependency at all.
*
!.gitignore
!*/
!*/.gitkeep
