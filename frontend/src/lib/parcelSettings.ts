// Fixed account identifiers for the courier's business-parcel service — not
// per-order data. Kept here (rather than inline in Home.tsx) so the future
// "parcel summary" feature can import the same constants instead of duplicating them.
export const PARCEL_CONTRACT_ID = '41503409';
export const PARCEL_BILLER_ID = '1679000994';
